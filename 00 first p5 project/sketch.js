// First Day Demo
// Devin Hubbard
// Sept 10, 2024
//
// Interactive programs -> dont finish...


function setup() {
  // happens one time at the start
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  // draw happens over and over, targets 60fps
  //background(220);
  circle(mouseX,mouseY,20);
}
